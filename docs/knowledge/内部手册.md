# 内部手册（用于验证 RAG）

这份文件只存在知识库里，没有写进 System Prompt。问「暗号」或「值班室分机」时，模型必须靠检索才能答对。

## 值班约定

- 暗号：落叶归根
- 值班室分机：8142
- 知识库负责人：林秋
- 私有文档检索走向量相似度，不要用网页搜索代替。

## V0.6 在本项目中的含义

RAG = 先把文档切成块并变成向量，提问时找出最像的几块，再交给 LLM 生成。
长期记忆（memories）记的是「用户是谁」；知识库（documents）记的是「资料里写了什么」。两者不要混用。
